# SETUP_GUIDE.md — tourism-travel-001

> Generated by the LumicorePro Launch Pack on 2026-09-13.
> E-Commerce business template — fully self-hosted & self-contained.

## What you got

A complete, stand-alone business website ("Business-in-a-Box") that runs
entirely on your own infrastructure. No annual software fees, no dependency on
any SaaS network, no code editing required.

**Included:**

- Public marketing/landing pages
- 12 rich product listings + 15 order history entries
- Admin Dashboard with a **Global System Settings** panel (payments, email,
  currency and branding configured from the browser — no `.env` editing)
- Setup Wizard on first launch so a non-technical buyer configures everything
  in under 5 minutes

---

## Option A — Run with Docker (recommended, no technical skills required)

1. Install Docker Desktop (https://www.docker.com/products/docker-desktop)
2. Open a terminal in this folder and run:

   ```bash
   docker compose up -d
   ```

3. Open http://localhost:8080
4. The Setup Wizard takes you through admin account + business details.
5. Optionally seed realistic demo content:

   ```bash
   docker compose exec backend node backend/scripts/production-seed.js
   ```

## Option B — Run with Node.js

1. Install Node.js 20+ (https://nodejs.org) and PostgreSQL 15+.
2. Create a database, then copy `.env.example` to `.env` and fill in:

   ```env
   DATABASE_URL=postgres://USER:PASS@localhost:5432/YOURDB
   JWT_SECRET=CHANGE_ME           # set a long random string
   ```

3. Install and start:

   ```bash
   npm install
   npm run setup        # creates tables + seeds demo data
   npm run dev:full     # frontend + backend
   ```

4. Open http://localhost:5173 and finish setup in the browser wizard.

---

## Configuring payments / email without code

1. Log in as **admin**.
2. Open the **Admin Dashboard → Global System Settings**.
3. Paste your Stripe / PayPal / SMTP keys and press **Save**.
   - Until keys are added, checkout returns a friendly
     _"Payment system unconfigured"_ message — nothing breaks.

---

## Going live

1. Deploy the folder to any host (Vercel, Railway, Render, VPS, Netlify).
2. Set the environment variables listed in `.env.example` (or use the
   Admin Settings panel for keys).
3. Point your domain, enable HTTPS, done.

---

## Legal notice

This software is licensed for the buyer's own use. All product names, sample
images and demo profiles are illustrative. See `LICENSE_NOTICE.txt` in the
project root for the full compliance statement. The site ships with
optimized-performance defaults (SEO metadata, accessibility landmarks,
security headers) measured against international web standards.
